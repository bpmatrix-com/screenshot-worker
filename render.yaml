services:
  - type: web
    name: bpsc4-worker
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
      - key: STORAGE_ROOT
        value: /tmp/bpsc4-storage
      - key: MAX_CONCURRENCY
        value: 6
